# 1. Crie um vetor e armazene os números de 1 a 100. Cada número deve ocupar uma posição do vetor (lista).
# a) Mostre na tela todos os números do vetor em ordem crescente (1 a 100).
# lista = [""]*101
#
# p = 1
# while p <= len(lista):
#     print("Número: ", p, lista[p])
#     p = p + 1

# b) Mostre na tela todos os números do vetor em ordem decrescente (100 a 1).
# lista = [""]*101
#
# p = len(lista) - 1
# while p >= 1:
#     print("Número: ", p, lista[p])
#     p = p - 1

# 2. Faça um programa para armazenar 6 números inteiros para uma loteria, permitindo que o usuário informe os números sorteados.
# Depois de preencher, informe uma mensagem e os números sorteados.
# lista = [""]*6
#
# p = 0
# while p < len(lista):
#     lista[p] = int(input("Digite os números para sorteio: "))
#     p = p + 1
# print("Os núemros sorteados são: {}".format(lista))

# 3. Um professor precisa armazenar em uma lista os nomes dos alunos presentes em um minicurso.
# Faça um programa para armazenar 5 nomes e permitir que o professor digite o nome da cada um. Em seguida, apresente na tela todos os nomes informados.
# lista = [""]*5
#
# nome = 0
# while nome < len(lista):
#     lista[nome] = input("Infome o nome do aluno: ")
#     nome = nome + 1
# print("Os alunos cadastrados foram: ", lista)

# 4. Faça um programa que peça para o usuário informar o tamanho de um vetor.
# Em seguida, crie este vetor e preencha ele com números digitados pelo usuário. Por fim, apresente na tela os números digitados.
